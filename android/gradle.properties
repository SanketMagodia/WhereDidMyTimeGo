org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:-UseCompressedOops -XX:+UseSerialGC -Xss512k
android.useAndroidX=true
kotlin.incremental=false
# Run each build in a fresh JVM — avoids residual memory from previous daemon
org.gradle.daemon=false